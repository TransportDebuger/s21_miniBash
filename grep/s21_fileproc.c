#include "s21_fileproc.h"

#include <stdio.h>

/*void printFile(char* fname, OptList* opt) {
    FILE* f = fopen(fname, "r");
    if (f) {
        char* linebuffer = NULL;
        while (1)
        {
            linebuffer = fgets()
        }
        
    }
    fclose(f);
}*/