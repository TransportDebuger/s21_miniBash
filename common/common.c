#include "common.h"

#include <stdlib.h>


struct modifier** getModifiers(int modc, char** modv) {
    struct modifier** modlist;
    modlist = malloc(modc*sizeof(struct modifier));
    int index = 1;
    while (index < modc) {
        
        index++;
    }
    return modlist;
}